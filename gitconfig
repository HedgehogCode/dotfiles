[core]
	editor = vim
	excludesfile = ~/.gitignore
[user]
	name = Benjamin Wilhelm
	email = benjamin.wilhelm@uni-konstanz.de
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
